 
Color and grayscale images with support for 16-bit (RGB565+dithering) and 24-bit (RGB888 and YUV888) modes, 
a) type 1 prediction (filtering) algorithm, 
b) lossless RLE compression, 
c) lossy compression (DCT transform + component subsampling), 
